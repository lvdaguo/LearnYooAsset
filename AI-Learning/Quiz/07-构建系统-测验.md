# 测验 · 07 · 构建系统

> **难度：中**（偏实战，少抽象。07 的内容本身偏编辑器工具链，题目往"真实项目打包时做什么决策"方向走）

---

## Q1. Collector 配置层级

配置文件的层级结构是 `XXX → YYY → ZZZ`，填出三层是什么：

```
AssetBundleCollectorSetting
  └── _________
        └── _________
              └── _________
```

### 我的答案
_（在此作答）_

---

## Q2. 三种 CollectorType 对号入座

| 场景 | 用哪种 CollectorType |
|------|-------------------|
| 一个 UI Prefab，业务代码会直接 Load 它 | _________ |
| 一个 Shader 文件，被 10 个 Material 引用，但代码不直接 Load | _________ |
| 一个 Texture，只被某个 Prefab 引用，代码不直接 Load | _________ |

### 我的答案
_（在此作答）_

---

## Q3. PackRule 选型

产品设计给的需求，你选什么 PackRule？

| 需求 | PackRule |
|------|---------|
| 每个角色 Prefab 单独一个 Bundle（精细控制换角色）| _________ |
| UI 图集按目录打包（`UI/Home/` 一个包，`UI/Battle/` 一个包）| _________ |
| 配置文件（JSON）不要压缩，直接当原生文件打 | _________ |
| Shader 变体单独打一个 Bundle | _________ |

### 我的答案
_（在此作答）_

---

## Q4. AddressRule 选型

三种默认的 AddressRule 各会产生什么形式的地址？

| AddressRule | 生成的地址样式 |
|-------------|-------------|
| `AddressByFileName` | _________ |
| `AddressByFilePath` | _________ |
| `AddressByGroupAndFileName` | _________ |

问：**哪种最容易冲突？** 为什么？

### 我的答案
_（在此作答）_

---

## Q5. 四条构建管线选型

对号入座：

| 场景 | 用哪条 Pipeline |
|------|--------------|
| 传统 AB 打包，兼容所有 Unity 版本 | _________ |
| 大项目需要增量构建，接受依赖 SBP | _________ |
| 纯文件原样打包（视频、JSON 不压缩）| _________ |
| 编辑器模拟模式下生成假 Manifest | _________ |

### 我的答案
_（在此作答）_

---

## Q6. 构建输出目录长啥样

你的 `BuildOutputRoot = "D:/CDN"`，`PackageName = "Default"`，`PackageVersion = "1.0"`。构建完后的目录结构是什么？（凭记忆画）

### 我的答案
_（在此作答）_

---

## Q7. BuildinFileCopyOption 三种策略选型

你要做一个游戏发行方案，对号入座：

| 场景 | CopyOption |
|------|-----------|
| 零安装包：2MB 的 APK 下载后进游戏再拉所有资源 | _________ |
| 全量首包：1GB 的 APK 内置所有资源，断网也能玩 | _________ |
| 混合方案：APK 内置首关资源（200MB），其他从 CDN 下 | _________ |

### 我的答案
_（在此作答）_

---

## Q8. CI/CD 脚本化构建

下面这段自动化构建代码少了一个**重要配置**（会导致大项目构建很慢），补上：

```csharp
var buildParameters = new BuiltinBuildParameters
{
    BuildOutputRoot       = "D:/CDN",
    BuildinFileRoot       = "Assets/StreamingAssets",
    BuildTarget           = BuildTarget.Android,
    PackageName           = "DefaultPackage",
    PackageVersion        = "1.0.0",
    FileNameStyle         = EFileNameStyle.HashName,
    CompressOption        = ECompressOption.LZ4,
    BuildinFileCopyOption = EBuildinFileCopyOption.ClearAndCopyAll,
    // ??? 少了什么？
};
```

提示：和"资源收集速度"有关。

### 我的答案
_（在此作答）_

---

## Q9. Task Runner 内部机制

BuiltinBuildPipeline 内部是一个 **Task Runner**，执行一系列 `IBuildTask`。以下两种需求，你会怎么插入自定义 Task？

- a) 打包完成后自动上传到 CDN（AWS S3 / 七牛云）
- b) 打包前检查所有 Prefab 是否有 missing reference

说思路即可。

### 我的答案
_（在此作答）_

---

## Q10. 实战场景题

你的项目结构：
- `Assets/UI/`（UI prefab、图集）
- `Assets/Characters/`（角色 prefab、模型、贴图）
- `Assets/Scenes/`（Unity 场景）
- `Assets/Audio/`（音频文件）
- `Assets/Config/`（JSON 配置文件）

请给出一个**合理的 Collector 配置方案**：
- 每个目录用哪种 CollectorType + PackRule + AddressRule？
- 理由是什么？

### 我的答案
_（在此作答）_

---

## AI 批改结果

_（AI 会在此填写评分与指正）_
