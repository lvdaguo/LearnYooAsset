# 测验 · 04 · 热更新完整流程

> **难度：进阶**（按你的背景调整过了，没有"什么是热更新"这种基础题）
> 答题时可以对比你以前自研系统的设计。

---

## Q1. 状态机流程还原

不翻文档，凭记忆写出 `PatchOperation` 的 8 个 FsmNode 的**执行顺序**（简写即可，如 `InitPackage → RequestVersion → ...`）。每个节点**失败时**应该回到哪个状态？

### 我的答案
_（在此作答）_

---

## Q2. 两次请求的本质区别

为什么要**分两步**做这件事？合并成一个 API `UpdateWithLatestVersionAsync()` 不是更简单吗？

```csharp
var versionOp = package.RequestPackageVersionAsync();   // 请求版本
yield return versionOp;
var manifestOp = package.UpdatePackageManifestAsync(versionOp.PackageVersion);  // 更新清单
yield return manifestOp;
```

（提示：想想**灰度发布**、**强制指定版本**、**版本回退**场景）

### 我的答案
_（在此作答）_

---

## Q3. 下载器创建的三种 API

`CreateResourceDownloader` 有三个重载：

```csharp
// A
package.CreateResourceDownloader(10, 3);

// B
package.CreateResourceDownloader(new string[]{"level1", "ui"}, 10, 3);

// C
package.CreateBundleDownloader(assetInfos, 10, 3);
```

分别对应什么场景？哪个最精细？

### 我的答案
_（在此作答）_

---

## Q4. EFileClearMode 有哪些模式？

不看文档，说出你记得的 `EFileClearMode` 枚举值。每个模式的适用场景是什么？

特别关注：**v2.3.18 新增的那个模式是干什么的？**

### 我的答案
_（在此作答）_

---

## Q5. 断点续传的"半死不活"场景

玩家在下载一个 50MB 的 Bundle，下载到 30MB 时：
- a) App 被系统杀掉
- b) 玩家主动杀 App 再进游戏

下次启动，YooAsset 会怎么处理这个 Bundle？  
（可以对比你自研 C++ FS 当年是怎么处理的）

### 我的答案
_（在此作答）_

---

## Q6. BuildinFileCopyOption 是干嘛的？

```csharp
buildParameters.BuildinFileCopyOption = EBuildinFileCopyOption.ClearAndCopyAll;
```

这个枚举有三个选项（`None / ClearAndCopyAll / ClearAndCopyByTags`），它**控制构建时的什么行为**？为什么叫 "Copy"？

（提示：跟"首包资源"有关）

### 我的答案
_（在此作答）_

---

## Q7. 设计题：灰度发布

老板要求：**新版本先灰度 10% 玩家**，线上跑 24 小时没问题再全量。

基于 YooAsset 怎么实现？（说思路即可，关键是 `RequestPackageVersionAsync` 这一步怎么改）

### 我的答案
_（在此作答）_

---

## Q8. 设计题：版本回退

上线后发现新版本有严重 bug，需要**紧急回滚到上一个版本**。  
- a) YooAsset 原生支持这个操作吗？
- b) 怎么做到？（CDN 目录结构 / IRemoteServices 实现怎么变）

### 我的答案
_（在此作答）_

---

## Q9. 错误还原

玩家反馈："我更新到 v2 了，但进游戏发现某些怪物模型还是 v1 的旧版本。"  
（其他玩家正常。）

你会怎么排查？可能的原因是什么？

### 我的答案
_（在此作答）_

---

## Q10. 对比题：YooAsset vs 你之前的自研系统

挑 1-2 个维度，说说 YooAsset 的设计和你之前做的 C++ FileSystem + Java/ObjC 客户端方案有什么**异同**？哪里让你觉得"原来这么做更好"或者"这样做反而不如我之前"？

可选对比角度：
- 状态机驱动 vs 回调链
- 异步操作的统一基类（GameAsyncOperation）
- Manifest 结构
- 文件校验时机
- 重试机制
- 多包管理

### 我的答案
_（在此作答）_

---

## AI 批改结果

_（AI 会在此填写评分与指正）_
